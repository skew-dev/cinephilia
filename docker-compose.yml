version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: cinephilia-pass-dev
      POSTGRES_USER: cinephiler-dev
      POSTGRES_DB: cinephiliadb
